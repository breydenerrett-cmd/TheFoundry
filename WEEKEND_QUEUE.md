# WEEKEND QUEUE — THE FOUNDRY (source of truth)

Status values: `READY` `RUNNING` `VERIFYING` `DONE` `BLOCKED_HUMAN` `BLOCKED_EXTERNAL` `FAILED_RETRYABLE` `FAILED_TERMINAL`

Rules: one task RUNNING at a time. A RUNNING row older than 90 min with no commit is reclaimed. Update this file after every completed or blocked task.

| ID | Task | Pri | Deps | Model | Status | Evidence required | Commit | Blocker / notes |
|---|---|---|---|---|---|---|---|---|
| W-01 | Create weekend control files (plan, queue, progress) | P0 | — | Fable | DONE | files in repo root, pushed | (this commit) | |
| W-02 | Create hourly Routine `Foundry Weekend Build Loop` (or documented fallback) | P0 | W-01 | Fable | BLOCKED_EXTERNAL | trigger id recorded in progress log | | Both `create_trigger` (hourly cron) and the `send_later` fallback were denied by the session's auto-mode permission classifier. Persistence is therefore **within-turn only**: the loop runs continuously in the current turn. **Brey (optional):** create Routine "Foundry Weekend Build Loop" (hourly, bound to this session) with the prompt in WEEKEND_AUTONOMOUS_PLAN.md, or allow trigger creation in permissions. |
| R-01 | Standalone repo: history pushed, builds, 55 tests pass | P0 | — | Fable | DONE | `main`@`db570b9` on origin; `cargo test` 55/55 in fresh clone | db570b9 | |
| R-02 | Close aisportsanalysis PR #1 with extraction note; sports repo unaffected | P0 | R-01 | Fable | DONE | PR closed, comment posted; sports branch head unchanged `91b0664`, clean tree | — | |
| R-03 | Rename repo `TheFoundry` → `the-foundry` | P0 | — | — | BLOCKED_HUMAN | repo metadata shows new name | | Proxy rejects repo-settings writes; no browser control here. **Brey:** GitHub → Settings → General → Repository name → `the-foundry`. ~10s. |
| R-04 | Set repo visibility PRIVATE | P0 | — | — | BLOCKED_HUMAN | `visibility: private` | | Same limitation. **Brey:** Settings → Danger Zone → Change visibility → Private. Nothing sensitive is in the repo meanwhile. |
| G-01 | Set up `gh` CLI + auth on Brey's Windows machine | P0 | — | — | BLOCKED_HUMAN | `gh auth status` output | | This session is a cloud sandbox with no access to the Windows machine or Chrome. **Brey:** `winget install GitHub.cli` then `gh auth login` (browser flow, token stays in Windows Credential Manager); then a local Claude Code session can drive `gh`. |
| G-02 | Document what GitHub ops are autonomous from this session vs need Brey | P0 | — | Haiku | DONE | section in WEEKEND_PROGRESS.md | (this commit) | |
| T-01 | Phase 4 truth-gate re-verification (all observers, bays, velocity, degraded remote, redaction, health, BREY_REQUIRED, fading/gone) — record a live text-mode run | P0 | R-01 | Sonnet | DONE | `cargo test` green + captured `foundry --no-remote` and fixture-fed floor output in progress log | see progress log | Live run found + fixed a truth bug: snapshot rendered "REMOTE ESTATE: live (0s)" because sync age = file read time, not capture time. Now `captured_at` sidecar/field drives age; missing → DEGRADED. 58 tests. |
| T-02 | Add CI: GitHub Actions `cargo fmt --check`, `clippy`, `cargo test` on push/PR | P0 | R-01 | Sonnet | DONE | workflow file, green run on `main` | 57c81b4 | CI run #1 success on 57c81b4 (fmt, clippy, test, no-remote smoke). |
| H-01 | Real Sports heartbeats: add `foundry_beat` to forward_capture/daily_loop/monitor_remote/test_parallel at safe boundaries, on the branch that owns them | P1 | T-01 | Sonnet | BLOCKED_HUMAN (code done) | diff on `claude/sports-betting-analysis-review-g1o0co` (new branch off it + PR, not in-place), plus Foundry reading ≥1 real line | aisportsanalysis 2d20f7c | Code complete: draft PR https://github.com/breydenerrett-cmd/aisportsanalysis/pull/2 (branch `claude/foundry-heartbeats-sports`). Helper verified fail-safe; Foundry reads emitted lines. **Brey:** merge PR #2 at a safe boundary (between routine fires) on the machine running the routines, then one fire produces real `.foundry/events.jsonl` lines — not merging into an actively-executing branch autonomously. |
| P-01 | Project resolution v2: explicit repo→bay map file + tags/rules, UNKNOWN bay preserved (no guessing), tests | P1 | T-01 | Sonnet | DONE | `bay.rs` rules file + tests | see progress log | `BayMap` from `foundry.bays.toml` (repos → rules → tags → UNRESOLVED). No more guessing: no-hint/unmatched → `▸ UNRESOLVED` group. 64 tests. |
| S-01 | Persistent state: SQLite snapshot + sequence numbers + restart recovery; prove restart does not fabricate healthy state | P1 | T-01 | Sonnet | DONE | tests incl. "restart-from-snapshot renders UNVERIFIED until fresh events" | see progress log | `persist.rs`: seq-numbered JSONL + atomic JSON snapshot (SQLite swappable later); restart marks every record `(restored)` → STALE/UNKNOWN until re-observed; TTL expiry (900s default); corrupted snapshot ignored with warning; secret-never-persisted test. 74 tests. |
| M-01 | Multi-machine foundation: `foundry-agent` binary (local observers → signed JSON over pluggable transport) + reducer ingest of `source=local_claude@host` | P1 | S-01 | Sonnet | DONE | agent emits, main ingests from file transport; auth test | see progress log | `foundry-agent` binary + HMAC-SHA256 signed bundles (`sign.rs`), `Publisher`/`Receiver` traits with `FileTransport` only, `AgentIngestObserver` rejecting bad sig/unknown key/replay/skew as visible degradation, MACHINES section, agent TTL. 88 tests. HTTP transport + key provisioning UX still open. |
| O-01 | Opus adversarial review of truth mapping before visuals (narrow scope: reducer + persistence + agent ingest) | P1 | S-01, M-01 | Opus→Sonnet | DONE | findings list, each fixed with a test | see progress log | Opus (~125k tokens) confirmed 7 lies with failing tests in `tests/opus_review.rs`: stale-log replay renders WORKING; future heartbeat → LAST OUTPUT 0s; seq-0 bundle replay resurrects dead machine; Degraded-empty-caps observer still "verified"; foreign heartbeat certifies canary; stale routine advertised as next; agents observer HEALTHY with unreadable transport. + security notes (key↔agent_id binding, unverified agent_id rows, unredacted error strings, unpersisted seq watermark). Sonnet fixer running. |
| V-01 | Static visual foundation: Tauri + React + Pixi shell, marquee, six bays, stations, state colors, observed/inferred, output shelf, fault beacon (fixture-driven) | P2 | T-01, T-02 | Sonnet | DONE | `npm run build` passes; screenshot via Playwright of fixture floor | see progress log | `app/`: Vite+React+Pixi v8 isometric floor, marquee, 7 bays, all states, observed/inferred/unknown treatment, output shelf (hatched = UNKNOWN), beacons, blind overlay when unverified/degraded; `#scene-mirror` DOM for truth tests; 4 Playwright tests; Tauri scaffold only (no webkit2gtk here). Visually sparse — polish is V-02+. |
| V-02 | 3-second glance: motion density, BREY_REQUIRED/FAILED beacons, STALE hatching, Opus plume, LAST OUTPUT / NEXT ROUTINE, counts | P2 | V-01 | Sonnet | DONE | screenshots per state | see progress log | Chunky chassis + light pools, ambient luminance = health, motion density (ghost outline for inferred, none for unknown), BREY/FAILED/HUNG beacons, Opus plume + `N OPUS`, 20px marquee in required order; `floor-idle.json`/`floor-blind.json` fixtures; 8 Playwright tests. Layout still under-scaled/off-center → V-03. |
| V-03 | All station states rendered; never animate unobserved work | P2 | V-02 | Sonnet | DONE | state-mapping table test | see progress log | `src/states.ts` single table (color/light/motion/glyph/beacon), restored→STALE enforced; fit-to-viewport with `data-scene-bounds` test at 1280×720 + 1920×1080; 3×3 bay grid; gradient/rim/scanline pass; `floor-states.json` fixture; 10 Playwright tests. Carry-over: label collisions, glyph draw still not reading `spec.glyph`. |
| V-04 | Modes: Command Center, Project Focus, Ambient, Incident, Deep Debug | P2 | V-03 | Sonnet | RUNNING (2026-09-05T00:25Z) | hotkeys, screenshots | | Also fix label collisions + glyph dispatch via table. |
| V-05 | Performance: measured idle CPU/RSS, frame-rate ladder, particle budget | P2 | V-04 | Sonnet | READY | numbers recorded | | |
| Z-01 | Write `WEEKEND_HANDOFF.md` | P0 | (end of weekend) | Fable | READY | file in repo | | Produce before autonomous mode ends. |
